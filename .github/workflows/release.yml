name: Create New Release

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: 'true'
      
      - name: Setup Xcode and Command Line Tools
        run: |
          sudo xcode-select --switch /Applications/Xcode_16.2.app/Contents/Developer
          sudo xcode-select --install || true
          xcodebuild -version
          xcodebuild -showsdks
          echo "Available iOS SDKs:"
          xcodebuild -showsdks | grep iphoneos

      - name: Install Build Dependencies
        run: |
          brew install llvm
          brew install libplist
          brew install pkg-config

      - name: Resolve Swift Dependencies
        run: |
          cd "$GITHUB_WORKSPACE/IDeviceKitten" && swift package resolve
          cd "$GITHUB_WORKSPACE/AltSourceKit" && swift package resolve
          cd "$GITHUB_WORKSPACE/NimbleKit" && swift package resolve
          cd "$GITHUB_WORKSPACE/Zsign" && swift package resolve

      - name: Clean Build Environment
        run: |
          make clean || true
          rm -rf ~/Library/Developer/Xcode/DerivedData
          rm -rf ~/Library/Caches/com.apple.dt.Xcode

      - name: Compile Portal
        run: |
          agvtool new-version -all $(git rev-list --count HEAD)
          make
          mkdir upload
          mv packages/* upload/

      - name: Get Version
        id: get_version
        run: |
          VERSION=$(/usr/libexec/PlistBuddy -c "Print CFBundleShortVersionString" Payload/*.app/Info.plist)
          echo "VERSION=${VERSION}" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: Portal v${{ steps.get_version.outputs.VERSION }}
          tag_name: v${{ steps.get_version.outputs.VERSION }}
          body: |
            Latest stable release to download

            Full Changelog: https://github.com/${{ github.repository }}/commits/v${{ steps.get_version.outputs.VERSION }}
          files: |
            upload/*ipa
          fail_on_unmatched_files: true
          draft: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
